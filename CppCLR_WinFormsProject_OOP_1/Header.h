#pragma once
#include <list>
#include <cmath>
#include <string>
#include <vector>
#include <string>
#include <random>
#include <chrono>
#include <time.h>
#include <istream>
#include <iomanip>
#include <sstream>
#include <fstream>
#include <iostream>
#include <stdlib.h>
#include <algorithm>
#include <exception>
#include <deque>;
#include <iterator>
#include <chrono>



using	std::cout;
using	std::cin;
using	std::endl;
using	std::setw;
using	std::setprecision;
using	std::fixed;
using	std::left;
using	std::right;
using	std::string;
using	std::vector;
using	std::ostream;
using	std::istream;
using	std::sort;
using	std::getline;
using	std::ifstream;
using	std::istringstream;
using	std::to_string;
using	std::runtime_error;
using	std::list;
using	std::deque;
using	std::ofstream;

using namespace	std::chrono;